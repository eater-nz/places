box: openjdk:8-jdk

build:
  steps:
      - script:
          name: prepare folder
          code: |
              mkdir output
              mkdir input
              shopt -s extglob
              mv !(input) input/
              ls -lah
      - script:
          name: get generator
          code: |
              git clone --depth 1 https://github.com/eater-nz/api-generator genrator
              ls -lah
      - wercker/maven:
          goals: install
          cache_repo: true
          version: 3.5.2
